{
  "extends": "./tsconfig.json",
  "include": ["src", "tests"],
  "compilerOptions": {
    // Override outDir to nothing, tests are never compiled to dist
    "outDir": null,
    "noEmit": true
  }
}